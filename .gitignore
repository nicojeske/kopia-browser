# Secrets — never commit
.env
*.env
!.env.example

# Build output
/kopia-browser
/kopia-browser.exe
/bin/
/dist/

# Go
*.test
*.out
coverage.*

# kopia cache / runtime
/cache/
/.kopia-cache/
*.config
# kopia metadata-cache spill (only appears if a relative cache dir is misused)
/blob-list/
/indexes/
/kopia.repository
/kopia.blobcfg
*.config.update-info.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
