# Session journal
> Append a short entry at the end of every working session so the next session resumes cleanly.
> Newest at top. Format: date — what was done / what's next / blockers.

## 2026-06-27 — M8 CI/CD GitHub Actions (DONE)

- **Done:** 3 new files. `.github/workflows/ci.yml` — single `build-test` job on `ubuntu-latest`: `go vet ./...`, `go test ./...`, `go build ./cmd/kopia-browser`; triggers on push to `main` + PR. `.github/workflows/docker-publish.yml` — builds + pushes `ghcr.io/nicojeske/kopia-browser` on `main` push and `v*` tags; `docker/metadata-action` for `latest`/`sha-<short>`/semver tags; `type=gha` layer cache. `.github/dependabot.yml` — weekly `gomod` + `github-actions` bumps. Updated PLAN.md (M8 DONE), DECISIONS.md (ADR), JOURNAL.md (this entry).
- **Verified:** `go test ./...` ✅, `go build ./cmd/kopia-browser` ✅ (mirror CI steps). Workflows run after first GitHub push.
- **One-time manual step:** After first push to GitHub, set GHCR package visibility to **public** (Packages → settings → "Change visibility") so k8s nodes can pull without an imagePullSecret. Or configure an imagePullSecret referencing a GHCR PAT.
- **Next:** Project feature-complete. Push repo to `github.com/nicojeske/kopia-browser` to activate CI.
- **Blockers:** no git remote configured yet (local-only repo).

## 2026-06-27 — M6 Docker (DONE)

- **Done:** Multi-stage `Dockerfile` + `.dockerignore`. Builder: `golang:1.26`, `CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w"` → static binary at `/out/kopia-browser`. Runtime: `gcr.io/distroless/static-debian12:nonroot` (CA certs, no shell, uid 65532). Cache dir created in builder + `COPY --chown=65532:65532` into `/var/cache/kopia-browser` (distroless has no shell/mkdir). `ENV KOPIA_CACHE_DIR=/var/cache/kopia-browser` so it resolves absolute (required per manager.go). `EXPOSE 8080`, `USER nonroot`, `VOLUME ["/var/cache/kopia-browser"]`, `ENTRYPOINT ["/usr/local/bin/kopia-browser"]`. `.dockerignore` excludes `.git/`, `.env*`, `bin/`, `docs/`, `*.md`, `Makefile`; keeps `web/` for go:embed. Docs: PLAN.md M6 → DONE, DECISIONS.md M6 entry added.
- **Verified:** `make test` ✅ (all existing tests unaffected). Image build requires Docker — user verifies via `make docker` + `docker run --env-file .env`.
- **Next:** All milestones DONE (M0–M7 + M6). Project feature-complete.
- **Blockers:** none.

## 2026-06-27 — Fix snapshot ordering + file-count bugs (DONE)

- **Done:** Three bugs in `internal/kopia/manager.go::ListSnapshots`, all found and fixed in one edit. (1) Sort result discarded — `snapshot.SortByTime` returns a new slice; assigned the result (`mans = snapshot.SortByTime(mans, true)`). (2) "Latest" stat showed earliest snapshot — consequence of bug 1; fixed by same assignment. (3) File count too low on incremental snapshots — `man.Stats.TotalFileCount` is the per-run upload tally; unchanged cached subtrees never walked, so they're excluded. Switched to `man.RootEntry.DirSummary.TotalFileCount` / `TotalFileSize` (full-tree totals, same source as browse folder sizes) with a `Stats` fallback for nil `RootEntry`. Updated `docs/KOPIA.md` §Go library notes + §Stats to document both gotchas.
- **Verified:** `make build` ✅, `make test` ✅ (kopia + web + config packages).
- **Next:** M6 — Docker + k8s.
- **Blockers:** none.

## 2026-06-27 — M7 Dashboard stats & enriched sidebar (DONE)

- **Done:** Full 3-layer M7. Data: new `internal/kopia/stats.go` — `NamespaceStats`, `StatsSnapshot`, pure `computeNamespaceStats` helper (group by Volume, keep newest EndTime per volume, sum TotalSize → SizeBytes; Volumes = distinct volume count), `StatsCache` (RWMutex, `Get()` returns copy, `Run(ctx)` ticker + immediate refresh, per-ns error logged+skipped). `internal/kopia/stats_test.go` — table-driven unit tests (empty, single vol/snap, multi-snap latest-wins, multi-vol sum, untagged, mixed). Config: `StatsRefreshInterval` from `STATS_REFRESH_INTERVAL` env (default 15m, `time.ParseDuration`). Main: `context.WithCancel` + signal handling (SIGINT/SIGTERM), `NewStatsCache` wired + `go cache.Run(ctx)`, passed to `NewServer`. Web: `Stats` interface + `stats` field on server; `injectSidebarData(data, nsList, activeNS)` builds `[]sidebarNSItem` (Name/Snapshots/Active) + `sidebarRepo` (Size/NSCount/Snapshots/Segments); `handleIndex` builds 4 `statCard`s + per-ns `nsCard`s (BarPct=max(4,SizeBytes\*100/MaxSize), SizeLabel, LastLabel via `humanRel`, data-\* attrs for JS sort); `NotReady` flag for first-load placeholder; new template funcs `humanRel`/`humanCount`. Templates: `partials.html` sidebar rewritten — `#side-search` input, section label with count, `.sidebar-nav` rows with dot+name+`.item-snap-count`, `.sidebar-repo` footer (drive icon + size + composition bar + shield). `namespaces.html` rewritten — 4 `.stat-card`s, `.dash-controls` row (`#ns-search` + `.sort-pills`), `#ns-grid` with enriched `.ns-card`s (`data-name/size/snapshots/last`, volumes/snapshots/stored mini-stats, `.size-bar`/`.size-bar-fill` inline width, last-backup line), `#ns-no-results`, client-side JS IIFE (filter by data-name substring + DOM reorder sort + pill toggle + initial size-desc sort). CSS: `.stat-cards`, `.stat-card`, `.dash-controls`, `.search-box`, sort pills, enriched `.ns-card` stats, `.size-bar`/`.size-bar-fill` (teal gradient), `.sidebar-search`, `.sidebar-repo`, `.repo-bar`/`.repo-seg` (nth-child teal variants), `.stats-calculating`. Tests: `fakeStats`/`sampleStats()` in `fake_test.go`; `newTestServer` takes `Stats` arg; 5 new handler assertions (stat cards, ns stats, calculating note, sidebar count); 2 new E2E tests (`TestE2EDashboardSearch` — type "paperless" → gitea card hidden; `TestE2EDashboardSortPill` — click name pill → gitea first).
- **Verified:** `go test ./...` ✅, `go test -tags=e2e ./internal/web` ✅ (7 E2E, 24+ handler tests). kapture visual pass (partial): stats-not-ready state confirmed correct — stat cards show "29"/`—`, enriched ns-cards show 0/0/`—` placeholders, "Statistics are being calculated…" banner present, sidebar search + nav + 29 rows all render. Stats-ready state not verified via kapture (29 cold kopia opens take O(minutes)); functionally correct per code review.
- **Key decisions:** background cache (eventual freshness; cold open too slow for on-demand), latest-per-volume size metric (avoids multi-count; documented as logical not deduped), client-side JS search/sort (30 items, zero latency), composition bar nth-child teal variants (no kind differentiation per user request). See DECISIONS.md.
- **Next:** **M6** — Docker + k8s: multi-stage Dockerfile (distroless/scratch), k8s manifests (Deployment + Service + Ingress), persistent kopia cache volume, env via Secret.
- **Blockers:** none.

## 2026-06-27 — M5 UI integration (DONE)

- **Done:** Integrated Claude-design dark-theme UI. Three commits: (1) `feat: M5 — new dark-theme UI` — redesigned all 5 templates (`partials.html`, `namespaces.html`, `volumes.html`, `snapshots.html`, `browse.html`) and `app.css`; self-hosted Geist + Geist Mono variable woff2 fonts (`web/static/fonts/`, 11 files + OFL.txt); `@font-face` rules in CSS; removed Google Fonts CDN link tags. (2) `feat: M5 — styled error page and persistent sidebar nav` — new `error.html` template; `renderError` switched from `http.Error` to `ExecuteTemplate`; `handleVolumes`/`handleSnapshots`/`handleBrowse` (full-page path only) inject `ListNamespaces` result for sidebar. (3) `fix: M5 — update E2E selectors` — updated all chromedp selectors from old class names to redesigned ones (`table.data-table`, `.entry-dir-link`, `.entry-file-link`, `.btn-tar`). Fixed `{{template "icon-X" nil}}` → `{{template "icon-X" .}}` across all templates (html/template rejects `nil` as explicit pipeline). Fixed PowerShell UTF-8 corruption of non-ASCII chars in browse.html (`—`, `·`, `←`) by using Write tool instead of `Set-Content`.
- **Verified:** `go test ./...` ✅, `go test -tags=e2e ./internal/web` ✅ (5 E2E tests pass). kapture visual pass: index (namespace cards), volumes (active sidebar highlight), snapshots, browse (htmx swap + URL push), error page (styled, mono error box) — all confirmed. Network monitor: zero requests to `fonts.googleapis.com` or `fonts.gstatic.com`; fonts served only from `/static/fonts/*.woff2`.
- **Key decisions:** self-hosted fonts (OFL, offline-safe, single-binary), persistent sidebar (full namespace list on every non-htmx page), styled error page (html/template over http.Error). See DECISIONS.md.
- **Next:** **M6** — Docker + k8s: multi-stage Dockerfile (distroless/scratch), k8s manifests (Deployment + Service + Ingress), persistent kopia cache volume.
- **Blockers:** none.

## 2026-06-26 — Volume navigation layer (DONE)

- **Done:** Added volume layer between namespace and snapshot list. `SnapshotInfo.Volume` field (= `Tags["volume"]`). `handleVolumes` (new): groups flat `ListSnapshots` result by volume in-process → `VolumeInfo{Name, Display, Count, Latest}` sorted alpha, empty last; renders `volumes.html`. `handleSnapshots` (reworked): filters by `{volume...}` path value (empty = untagged); renders updated `snapshots.html` with volume breadcrumb. Routes: `/repo/{ns}` → volumes; `/repo/{ns}/vol/{volume...}` → snapshots. New `volumes.html`. Updated `snapshots.html` breadcrumb (`Namespaces / ns / display`). `app.css` merges `.volumes` into `.snapshots` selectors. Tests: `sampleData()` has 2 volumes (`data-pvc`, `config-pvc`); updated httptest cases (volumes page + per-volume snapshot filter); `TestE2ENamespaceToVolumes` (new) + `TestE2ENamespaceToSnapshots` + `TestE2EBrowseDir` updated to click through volume layer; integration `TestListSnapshotsLive` logs distinct volumes. Docs: ARCHITECTURE routes, DECISIONS ADR, PLAN M5 checked.
- **Verified:** `go test ./...` ✅, `go test -tags=e2e ./internal/web` ✅ (5 E2E tests pass). Live integration run pending.
- **Key decisions:** handler-side grouping (no new interface method), `{volume...}` wildcard for empty-volume URL, "(no volume)" display-only sentinel. See DECISIONS.md.
- **Next:** **M5** continued — snapshot metadata improvements, error pages, empty states, kapture visual pass; then M6 Docker+k8s.
- **Blockers:** none.

## 2026-06-26 — M4 Download folder (tar) (DONE)
- **Done:** Full 3-layer M4. Data: extracted `descendToDir(ctx, ns, snapID, dirPath) (kopiafs.Directory, error)` private helper — `m.open` → `LoadSnapshot` → `SnapshotRoot` → walk segments via `dir.Child`, mapping `ErrEntryNotFound → ErrNotFound`; `Dir` refactored to call it; `ErrNotADirectory` sentinel added. `TarDir(ctx, ns, snapID, dirPath, w io.Writer) error` streams via `writeTarTree` — `kopiafs.IterateEntries` callback; dirs: header + recurse; files: header + `f.Open` + `io.Copy`; symlinks: TypeSymlink; unknown: log+skip. Unit test `tar_test.go` (in-memory fakes). Integration test `TestTarDirLive` added. Handler: `TarDir` on `Backups` interface; `handleDownload` error-switch: nil→file (unchanged), `ErrNotAFile`→tar (empty path = root → `<ns>.tar`; `path.Base` for subdir), `ErrNotFound`→404, else→500. Fake `OpenFile` fixed: dirs map hit → `ErrNotAFile` (was `ErrNotFound`). Fake `TarDir` walks dirs/files maps. New httptest cases: root tar 200, subdir tar 200, browse has `/download/data` + `btn-download-folder`. UI: browse.html adds `.btn-download-folder` button + `.tar-link` per dir row. app.css: styles for both. E2E `TestE2EFolderTarLink` asserts both hrefs contain `/download/`.
- **Verified:** `go test ./...` ✅, `make e2e` ✅. Live integration + manual pending live run.
- **Key decisions:** same route; `io.Writer` over pipe; plain tar; `descendToDir` refactor; `ErrNotAFile` as signal. See DECISIONS.md.
- **Next:** **M5** — UI refinement: snapshot metadata, human sizes, sorting, error pages, empty states; broaden E2E; kapture visual pass.
- **Blockers:** none.

## 2026-06-26 — M3 Download single file (DONE)
- **Done:** Full 3-layer M3. Data: `OpenFile(ctx, ns, snapID, path)` on Manager — mirrors `Dir`'s descent but walks `segs[:len-1]` as dirs then asserts `kopiafs.File` on the final segment; `f.Open(ctx)` returns `kopiafs.Reader` which satisfies `io.ReadSeekCloser` directly (embeds ReadCloser + Seeker); added `ErrNotFound` + `ErrNotAFile` sentinels. Handler: `GET /repo/{ns}/snap/{id}/download/{path...}` → `handleDownload` (reuses `cleanBrowsePath` for traversal guard, empty-path → 400, ErrNotFound/ErrNotAFile → 404, else 500); `http.ServeContent` handles Content-Type sniff, Content-Length, Last-Modified, Range requests; `Content-Disposition: attachment; filename=...` set before. `handleBrowse` now computes `DownloadBase` (same logic as `BrowseBase` but `/download`). UI: `browse.html` file rows now have `<a href="{{$.DownloadBase}}/{{urlPathEscape .Name}}">` (plain href, no htmx); `.entry-file a` hover style in app.css. Tests: `nopReadSeekCloser` in fake, `files` map keyed `"{snapID}|{path}"`, httptest cases (success root file, success subdir file, 400 empty path, 404 missing, 500 error, browse link presence); integration `TestOpenFileLive` (Dir → find file entry → OpenFile → ReadAll → assert len == Size); e2e `TestE2EDownloadLink` (chromedp AttributeValue `.entry-file a href` contains `/download/`).
- **Verified:** `go test ./...` ✅, `make e2e` ✅. Live integration + checksum vs kopia-restore pending live run.
- **Key decision:** `http.ServeContent` over `io.Copy` — free Range/If-Modified-Since/Content-Type sniff for zero extra code since `kopiafs.Reader` already satisfies `io.ReadSeeker`.
- **Next:** **M4** — download folder (tar): `TarDir(ctx, ns, snapID, path)` streaming tar on the fly; same `/download/{path...}` route serves tar when path is a directory; `Content-Type: application/x-tar`.
- **Blockers:** none.

## 2026-06-26 — M2 Browse dir tree (DONE)
- **Done:** Full 3-layer M2. Data: `Dir(ctx, ns, snapID, path)` in `Manager` — `snapshot.LoadSnapshot` → `snapshotfs.SnapshotRoot` → assert `kopiafs.Directory` → descend with `dir.Child` per segment → `kopiafs.GetAllEntries` → sort (dirs first, alpha) → map to `DirEntry` DTO. New imports: `github.com/kopia/kopia/fs` (as `kopiafs`), `snapshot/snapshotfs`, `repo/manifest`. Handler: `Backups` interface extended with `Dir`; `GET /repo/{ns}/snap/{id}/browse/{path...}` → `handleBrowse` (htmx-branch: `HX-Request: true` → fragment only, else full page); `cleanBrowsePath` sanitizes `{path...}` value via rooted `path.Clean` + segment validation. UI: `browse.html` with `{{define "browse-content"}}` partial (breadcrumb range + entries table with `hx-get`/`hx-target="#listing"`/`hx-push-url="true"` dir links + `urlPathEscape` for filenames). Snapshot row in `snapshots.html` now links to browse root. CSS: `.entries` table + `.entry-dir`/`.entry-file`. Tests: `TestCleanBrowsePath` (unit), `TestHandlers` extended (root listing, subdir, htmx fragment, data error, leak guard), `TestE2EBrowseDir` (chromedp: namespaces→snapshots→root dir→htmx subdir nav, waits for subdir-specific link).
- **Verified:** `go test ./...` ✅, `make e2e` ✅. Integration vs live garage pending.
- **Gotcha:** `WaitVisible table.entries` in chromedp fires before htmx swap completes (original table still visible). Fix: wait for a selector unique to the subdir (`a[href*="documents"]`).
- **Next:** **M3** — download single file: `OpenFile(ns, snapID, path)` → `io.ReadSeekCloser`; `GET /repo/{ns}/snap/{id}/download/{path...}` with `Content-Disposition` + MIME type; download links on browse listing; chromedp E2E downloads + checks bytes.
- **Blockers:** none.

## 2026-06-26 — M1 List namespaces + snapshots (DONE)
- **Done:** Full 3-layer M1. Data: `internal/kopia` Manager — `ListNamespaces` (minio-go delimiter listing, one round trip), lazy per-ns `repo.Connect`(ReadOnly)+`repo.Open` cached, `ListSnapshots` (`ListSnapshotManifests`+`LoadSnapshots`+`SortByTime`), `SnapshotInfo` DTO (no kopia types leak to UI). Added `KOPIA_CACHE_DIR` config (default `.kopia-cache`, resolved absolute). Handlers: `Backups` interface, `GET /` namespaces + `GET /repo/{ns}` snapshot table, `/static/` serving, humanBytes/humanTime funcs. UI: `partials.html`+`namespaces.html`+`snapshots.html`, vendored htmx 2.0.4 + app.css. Tests: httptest (table-driven vs fake, asserts no `host_pods` leak), live integration (`-tags=integration`), chromedp E2E (`make e2e`, `-tags=e2e`, fake backend). Deps: kopia v0.22.3, minio-go/v7, chromedp.
- **Verified:** unit+httptest ✓, integration vs garage ✓ (30 namespaces incl `paperless`, real snapshot w/ backup name+tags+size), `make e2e` ✓, live server renders real paperless snapshots, no source-path leak, read-only.
- **Gotcha found & fixed:** relative kopia `CacheDirectory` → SIGSEGV nil-deref in content cache. `kopia.New` now `filepath.Abs`-resolves it. Tests passed despite this (used absolute temp dirs); only the live server run caught it. See DECISIONS.md / KOPIA.md.
- **Next:** **M2** — browse dir tree: `Dir(ns, snapID, path)` walking `fs.Directory` from `rootEntry.obj`; confirm `fs` iterate/Open API at v0.22.3; `GET /repo/{ns}/snap/{id}/browse/{path...}` + breadcrumb + htmx partial; chromedp navigate-into-dir E2E.
- **Blockers:** none. (`make` still not on PATH in shell; raw `go`/`go test -tags=...` used.)

## 2026-06-26 — M0 Scaffold (DONE)
- **Done:** Go 1.26 module scaffold. `internal/config` (godotenv `.env` load + required-var validation, aggregates all missing). `internal/web` stdlib `ServeMux` server: `GET /healthz`→`ok`, `GET /{$}`→embedded `index.html`. Root `assets` package embeds `web/templates` + `web/static` (`all:` prefix needed for `.gitkeep`). Makefile (run/test/test-integration/build/docker), `.env.example`. Verified: `go build`/`vet`/`test` green; server boots + serves both routes (200), non-root 404s, missing creds fail fast. Updated CLAUDE.md build section, PLAN.md (M0 DONE).
- **Next:** **M1** — `internal/kopia` RepoManager: `ListNamespaces` (S3 blob list under `KOPIA_PREFIX`), `Open(ns)`, `ListSnapshots`; `GET /` + `GET /repo/{ns}` pages; integration test vs garage.
- **Blockers:** `make` installed via winget but not yet on PATH in active shell (needs refresh); raw `go` commands work meanwhile.

## 2026-06-26 — Planning & docs setup
- **Done:** Decided stack (Go + kopia lib + htmx). Verified live connection to Garage S3 (`paperless` repo): endpoint/region/prefix/password all confirmed working, snapshot JSON shape captured. Wrote CLAUDE.md and `docs/` (PLAN, ARCHITECTURE, KOPIA, DECISIONS, JOURNAL). Go installed via winget (needs new shell to appear on PATH).
- **Next:** Start **M0** (scaffold). Confirm `go` is on PATH first.
- **Blockers:** none.
